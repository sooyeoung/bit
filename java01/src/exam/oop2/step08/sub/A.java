/* 캡슐화: 접근 제한
 * - 멤버(변수,메서드) => private, (default), protected, public
 * 	 1) private : 그 클래스 내부에서만 접근 가능
 *   2) (default): 그 클래스 내부 + 같은 패키지의 클래스
 *   3) protected: 그 클래스 내부 + 같은 패키지의 클래스 + 서브(자손) 클래스
 *   4) public: 공개 
 * - 클래스 => public, (default)
 * 	 1) (default): 같은 패키지의 클래스만 접근 가능
 *   2) public: 공개
 * -  
 */
package exam.oop2.step08.sub;

/* 같은 패키지만 접근 가능 */
class A {
	
}
















